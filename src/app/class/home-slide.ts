export class HomeSlide {
    id: number;
    image: string
    text: string;

    constructor(id: number, image: string, text: string) {
        this.id = id;
        this.image = image;
        this.text = text;
    }
}